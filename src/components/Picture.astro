---
import { Image } from 'astro:assets'

const { src, alt } = Astro.props
---

<figure class="flex flex-col items-center">
    <Image class="max-w-4xl rounded" src={src} alt={alt} />
    <figcaption class="text-center">
        <slot />
    </figcaption>
</figure>
